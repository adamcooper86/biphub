<section id="adminPanel" class="row">
  <div class="col-sm-1"></div>
  <div class="col-sm-10">
    <h1><%= link_to("Admin Panel", admin_path(current_user)) %> > <%= link_to(@school.name, school_path(@school)) %> > New School Coordinator</h1>
    <div class="row">
      <div class="col-sm-2"></div>
      <div class="col-sm-7">
        <%= render 'coordinators_form' %>
      </div>
      <div class="col-sm-3"></div>
  </div>
  <div class="col-sm-1"></div>
</section>
