<%= form_for [@school, @student] do |f| %>

  First Name: <%= f.text_field :first_name %>
  Last Name: <%= f.text_field :last_name %>
  Special Education Teacher: <%= f.select(:speducator_id, speducator_options(@speducators)) %>

  <%= f.submit "Submit" %>

<% end %>